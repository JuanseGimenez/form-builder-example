<div class="col-6 mx-auto">
  <div class="card">
    <div class="card-body">
      <%= form_with(model: task, builder: FormBuilders::Custom) do |form| %>
        <%= form.text_field :title %>
        <%= form.text_area :description %>
        <%= form.text_field :user %>
        <%= form.submit %>
      <% end %>
    </div>
  </div>
</div>
